% package =================================================%
\usepackage[dvipdfmx]{graphicx} % 図の挿入を可能に
\usepackage[deluxe]{otf} % 明朝体を可能に
\usepackage[dvipdfmx]{graphicx} % figure
\usepackage{amsmath,amssymb} % equation
\usepackage{bm} % bold font
\usepackage[numbers]{natbib} % reference
\usepackage{titlesec} % sectionのフォントサイズを変更
%=========================================================%

% design =============================================%
\usepackage[top=20truemm,bottom=25truemm,left=20truemm,right=20truemm]{geometry} % 余白
\setlength{\columnsep}{7truemm} % 二段組中央
\setlength{\baselineskip}{14pt} % 行間
\titleformat{\section}[block]{\normalfont\large\bfseries}{\thesection}{1em}{} % セクションのフォントサイズ
%=========================================================%

% title ==================================================%
\makeatletter % @の変数を使用可能にするコマンド
\newcommand{\type}[1]{\gdef\@type{#1}} % 文書種類
\newcommand{\university}[1]{\gdef\@university{#1}} % 大学名
\newcommand{\department}[1]{\gdef\@department{#1}} % 学部名
\newcommand{\course}[1]{\gdef\@course{#1}} % 所属専攻
\newcommand{\chair}[1]{\gdef\@chair{#1}} % 所属グループ
\newcommand{\grade}[1]{\gdef\@grade{#1}} % 学年
\newcommand{\studentidnumber}[1]{\gdef\@studentidnumber{#1}} % 学籍番号
\newcommand{\enauthor}[1]{\gdef\@enauthor{#1}} % 名前（英語ver.）
\newcommand{\supervisor}[1]{\gdef\@supervisor{#1}} % 指導教官
\def\maketitle{
    \twocolumn[
        \begin{center}
            \fontsize{14pt}{18pt} \bfseries
            \@title
            \par
            \vspace{15pt}
            \fontsize{10pt}{14pt} \normalfont
            \begin{tabular}{|p{20truemm}|p{50truemm}|p{20truemm}|p{50truemm}|}
                \hline
                学生番号 & 282251265 & 氏名 & 志村　駿
                \\ \hline
                所属専攻 & 機械システム工学専攻 & 指導教員 & 竹内　一郎
                \\ \hline
            \end{tabular}
        \end{center}
        \vspace{10pt}
    ]
}
\makeatother
%=========================================================%