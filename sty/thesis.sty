

% package =================================================%
\usepackage[dvipdfmx,hidelinks]{hyperref} % hyper reference
\usepackage[dvipdfmx]{graphicx} % figure
\usepackage{amsmath,amssymb} % equation
\usepackage{bm} % bold font
\usepackage{fancyhdr} % header / footer
\usepackage{cite} % reference citation
\usepackage{algorithm} % pseudo code
\usepackage[noEnd=false]{algpseudocodex} % pseudo code (noEnd indicates indent style)
\usepackage{bbm} % indicator factor 
\usepackage{enumitem} % change design of enumeration
\usepackage{subcaption} % to arrange two or more figures in a row
\usepackage{mathtools} % for definition ":="
\usepackage{pxjahyper} % しおりの日本語文字化け対応
%=========================================================%

% title ==================================================%
\makeatletter % @で始まる変数を使用可能にする（\makeatotherで締める）
% title page用の変数定義
\newcommand{\type}[1]{\gdef\@type{#1}} % 文書種類
\newcommand{\university}[1]{\gdef\@university{#1}} % 大学名
\newcommand{\department}[1]{\gdef\@department{#1}} % 学部名
\newcommand{\course}[1]{\gdef\@course{#1}} % 所属専攻
\newcommand{\chair}[1]{\gdef\@chair{#1}} % 所属グループ
\newcommand{\grade}[1]{\gdef\@grade{#1}} % 学年
\newcommand{\enauthor}[1]{\gdef\@enauthor{#1}} % 名前（英語ver.）
\newcommand{\supervisor}[1]{\gdef\@supervisor{#1}} % 指導教官
% title page の設定
\def\maketitle{ 
    \begin{titlepage}
        \linewidth=\fullwidth
        \setlength{\voffset}{-20pt}
        \setlength{\hoffset}{-40pt}
        \addtolength{\textheight}{250mm}
        \pagestyle{empty}
        \hsize 45zw
        \begin{center}
            % title
            \huge \bfseries 
            \@title
            \par
            \vspace{280pt}
            % supervisor
            \Large \normalfont
            指導教官 
            \quad 
            \lineskip .75em
            \LARGE
            \begin{tabular}[t]{l}
                竹内　一郎
            \end{tabular}
            \par
            % belong
            \Large 
            \vspace{60pt}
            \@university \ \@department
            \par \vspace{3pt}
            \@course
            \par \vspace{3pt}
            \@chair
            \par \vspace{10pt}
            \@grade \quad 
            % name
            \bfseries \LARGE
            \@author 
            \par \vspace{30pt}
        \end{center}
    \end{titlepage}
    % authority page
    \newpage
    \thispagestyle{empty}
    \setlength{\voffset}{0pt}
    \null \vspace{450pt} % 本文の内容を空で埋める
    \begin{flushleft} % 左寄せの内容
        \Large
        Copyright 
        \copyright % © のこと
        \enskip % 大きめの空白
        \@enauthor
        \par \vspace{10pt}
        \@date
    \end{flushleft}
}
\makeatother
%=========================================================%

% header footer ==========================================%
\pagestyle{fancy}
\fancyhf{}
\fancyhead[RO]{\rightmark}
\fancyfoot[RO]{\thepage}
\fancyhead[LE]{\leftmark}
\fancyfoot[LE]{\thepage}
%=========================================================%


% parameters =============================================%
% 本文幅の調整
\setlength{\textwidth}{13cm}
% マージンの調整（偶数ページと奇数ページで別に決める）
\setlength{\oddsidemargin}{20pt} % 右ページの左側
\setlength{\evensidemargin}{-20pt} % 左ページの左側
\setlength\voffset{-20pt}
\setlength{\textheight}{530pt}
\setlength\footskip{2\baselineskip} % 本文の最後とフッターとの距離
% counter類
\setcounter{secnumdepth}{2} % [-1]:part(章)・[0]:chapter(部)・[1]:section(節)・[2]:subsection(項)・[3]:subsubsection(目)・[4]:paragraph(段落)
\setcounter{tocdepth}{2} % 目次に出す深さ
%=========================================================%

% (re)new ==================================================%
\fancypagestyle{fancy2}{
    % ヘッダーとフッターの設定
    \fancyhf{} % ヘッダーとフッターを初期化
    \fancyfoot[RO]{\thepage} % ページ番号
    \fancyfoot[LE]{\thepage} % ページ番号
    \renewcommand{\headrulewidth}{0pt} % 横線消す
}
% pseudocodeのinputとoutput表示
\renewcommand{\algorithmicrequire}{\textbf{Input:}}
\renewcommand{\algorithmicensure}{\textbf{Output:}}
% % Procedure を Function にカスタマイズ
% \renewcommand{\algorithmicprocedure}{\textbf{Function}}
% \renewcommand{\algorithmicendprocedure}{\textbf{EndFunction}}
% pseudocodeのインデント幅調整
\algrenewcommand\algorithmicindent{3em}
% 図を1ページに複数入れるための設定
\renewcommand{\floatpagefraction}{1.0} % 図だけのページの割合を80%に設定
\renewcommand{\textfraction}{0.0}     % 本文の最小割合を10%に設定
\renewcommand{\topfraction}{0.9}      % ページ上部の図の最大割合を90%に設定
\renewcommand{\bottomfraction}{0.9}   % ページ下部の図の最大割合を90%に設定